# Analysis Service (Python)

A python service that makes use of Google's Tensorflow to perform inferential analysis of activity in the experiment engine.

## Analysis Setup

To install all requirements:

```
pip install -r requirements.txt
```

To start the service:

```
./run.sh
```
